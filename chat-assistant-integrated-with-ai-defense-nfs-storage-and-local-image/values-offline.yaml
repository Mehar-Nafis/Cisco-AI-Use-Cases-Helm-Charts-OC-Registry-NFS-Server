# ============================================================================
# CHAT-ASSISTANT (AI DEFENSE) FAILOVER OVERLAY
# Apply on top of values.yaml when the in-cluster mirror (nim-mirror
# namespace in the OpenShift internal registry — this chart's default, see
# values.yaml) is unavailable:
#   helm upgrade chat-assistant-aidefense . -f values.yaml -f values-offline.yaml \
#     --set ngc.apiKey=<YOUR_NGC_KEY> -n chat-assistant-aidefense
#
# Repoints the 4 NIM images at the GitLab Container Registry on the 1 TB box
# — a second, independent copy kept outside the cluster. Registry =
# 198.18.134.23:5050, project = root/model-images (self-signed / plain HTTP).
# Same pattern as the insurguard chart's values-offline.yaml. llama shares
# the exact same image+tag mirrored for the vss-helm chart, same digest.
# ============================================================================
images:
  llama: "198.18.134.23:5050/root/model-images/llama-3.1-8b-instruct:1.8.6"
  embedqa: "198.18.134.23:5050/root/model-images/nv-embedqa-e5-v5:1.6"
  content: "198.18.134.23:5050/root/model-images/llama-3.1-nemoguard-8b-content-safety:1.10.1"
  topicControl: "198.18.134.23:5050/root/model-images/llama-3.1-nemoguard-8b-topic-control:1.10.1"
